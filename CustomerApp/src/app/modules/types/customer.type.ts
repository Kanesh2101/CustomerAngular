export interface Customer {
  email: string;
  firstName: string;
  lastName: string;
  contactNo: number;
  address: string;
}

export interface CustomerDTO {
  email: string;
  firstName: string;
  lastName: string;
  contactNo: string;
  address: string;
}
